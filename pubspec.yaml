name: double_a
description: "Plane Deinen Stundenplan"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.2

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.7.0
  cookie_jar: ^4.0.8
  dio_cookie_manager: ^3.1.1
  html: ^0.15.4
  animated_custom_dropdown: ^3.1.1
  syncfusion_flutter_calendar: ^28.1.38
  syncfusion_localizations: ^28.2.9
  device_calendar: #^4.3.3
    git:
      url: https://github.com/0niel/device_calendar

  flex_color_scheme: ^8.1.1
  smooth_page_indicator: ^1.2.0+3
  
  flutter_localizations:
    sdk: flutter
  timezone: ^0.10.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/ca/
    - assets/splash/